v<-c(1,4,3,5,2)
sort_result<-sort(v)
print(sort_result)

sort_result<-sort(v,decreasing=TRUE)
print(sort_result)

color <-c("red","resulr","resulo","resulp")
sort_result<-sort(color)
print(sort_result)

sort_result<-sort(color,decreasing=TRUE)
print(sort_result)
